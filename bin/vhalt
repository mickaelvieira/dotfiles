#!/bin/bash

set -o nounset
set -o errexit
set -o pipefail
#set -x

if [[ -z "${PATH_VM}" ]]
then
    echo "The path of the current vagrant machine [PATH_VM] has not been assigned"
    exit
fi

cd "${PATH_VM}"

vagrant halt

